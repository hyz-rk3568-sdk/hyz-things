# Rust Hello World

Cross-compiled for the RK3568 Buildroot target using the Buildroot-generated AArch64 glibc toolchain.

Build from the workspace root:

```sh
./scripts/build-rust-hello.sh
```

The output is intentionally kept under `target/` and must not be committed.
