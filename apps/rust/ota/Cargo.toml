[package]
name = "hyz-ota"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
sha2 = "0.11.0"
ureq = { version = "3.3.0", default-features = false, features = ["rustls"] }

[profile.release]
strip = true
lto = "thin"
